<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pikchillytechnologies.engineeingacademy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ArticlesActivity"></activity>
        <activity android:name=".AnswersActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".ExamActivity" />
        <activity android:name=".ExamInstructionActivity" />
        <activity android:name=".ExamListActivity" />
        <activity android:name=".SubCoursesActivity" />
        <activity android:name=".CoursesActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>